<UserControl x:Class="ImageProcessingView.Images"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageProcessingView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="470" d:DesignWidth="950">
    <Grid>
        <materialDesign:Card VerticalAlignment="Bottom" Height="460" Width="460" Margin="0,0,480,5" HorizontalAlignment="Right">
            <Image x:Name="inputImage" Source="{Binding BitmapImageInput}" />
        </materialDesign:Card>
        <materialDesign:Card VerticalAlignment="Bottom" Height="460" Width="460" Margin="0,0,5,5" HorizontalAlignment="Right">
            <Image x:Name="outputImage" Source="{Binding BitmapImageOutput}" />
        </materialDesign:Card>
    </Grid>
</UserControl>
